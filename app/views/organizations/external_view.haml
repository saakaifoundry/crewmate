%h2= t('.title', :name => h(@organization))

%p= t('.you_cant_access')

%br

- if @organization.admins.any?
  %p= t('.request_permission')

  .users.users_admins
    - @organization.admins.each do |user|
      %p.user
        = micro_avatar(user)
        = link_to h(user), user
        .clear
